name: test
permissions: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches:
      - master

  pull_request:

jobs:
  matrix:
    name: Configure matrix
    runs-on: ubuntu-slim
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - name: Configure test matrix
        id: set-matrix
        env:
          EVENT_NAME: ${{ github.event_name }}
        run: |
          if [ "$EVENT_NAME" = "pull_request" ]; then
            echo 'matrix={"os": ["ubuntu-latest"], "node-version": ["22", "24", "26"], "include": [{"os": "windows-latest", "node-version": "24"}]}' >> "$GITHUB_OUTPUT"
          else
            echo 'matrix={"include": [{"os": "ubuntu-latest", "node-version": "22"}, {"os": "ubuntu-latest", "node-version": "24"}, {"os": "ubuntu-latest", "node-version": "26"}, {"os": "windows-latest", "node-version": "24"}, {"os": "macos-latest", "node-version": "24"}]}' >> "$GITHUB_OUTPUT"
          fi

  lint:
    name: Lint
    runs-on: ubuntu-slim
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install pnpm
        uses: pnpm/setup@84cb39b217b10273981911c288cd62326dc7c6d2 # v2.0.2
        with:
          runtime: node@24
          cache: true

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run lint
        run: pnpm lint

      - name: Check committed types are up to date
        run: git diff --exit-code -- '**/types/*.d.ts'

  test:
    name: Test
    needs: [matrix, lint]
    runs-on: ${{ matrix.os }}
    strategy:
      matrix: ${{ fromJSON(needs.matrix.outputs.matrix) }}

    steps:
      - name: Setup Git
        if: matrix.os == 'windows-latest'
        run: git config --global core.autocrlf input

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Install pnpm
        uses: pnpm/setup@84cb39b217b10273981911c288cd62326dc7c6d2 # v2.0.2
        with:
          runtime: node@${{ matrix.node-version }}
          cache: true

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run unit tests
        if: matrix.os != 'ubuntu-latest' || matrix.node-version != '24'
        run: pnpm test:unit

      - name: Run integration tests
        if: matrix.os != 'ubuntu-latest' || matrix.node-version != '24'
        run: pnpm test:integration

      - name: Run all tests with coverage on latest Node on Linux
        if: matrix.os == 'ubuntu-latest' && matrix.node-version == '24'
        run: pnpm test:coverage

      - name: Publish code coverage report
        if: matrix.os == 'ubuntu-latest' && matrix.node-version == '24'
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7.0.0
        with:
          name: codecov

  site:
    name: Site
    needs: lint
    runs-on: ubuntu-slim
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install pnpm
        uses: pnpm/setup@84cb39b217b10273981911c288cd62326dc7c6d2 # v2.0.2
        with:
          runtime: node@24
          cache: true

      - name: Install site dependencies
        run: pnpm --dir site install --frozen-lockfile

      - name: Test site
        run: pnpm --dir site test
